# ── Dependencies ──
node_modules/
.dart_tool/
.packages

# ── Flutter / Dart ──
build/
*.iml
*.lock
!pubspec.lock
!package-lock.json
!backend/package-lock.json
.flutter-plugins
.flutter-plugins-dependencies

# ── Environment & Secrets ──
.env
.env.*
!.env.example
*.key
*.pem

# ── Firebase ──
*-firebase-adminsdk-*.json
firebase-service-account.json
.firebase/

# ── Vercel ──
.vercel/

# ── IDE ──
.idea/
.vscode/
*.swp
*.swo
*~

# ── OS ──
.DS_Store
Thumbs.db
desktop.ini

# ── Logs ──
*.log
npm-debug.log*

# ── Coverage ──
coverage/

# ── Build scripts with secrets (safety net) ──
build-apk.bat
run.bat
